apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-deployment
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: vincentsit722acr.azurecr.io/order_service:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@db:5432/$(DATABASE_NAME)"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef: { name: ecomm-secrets-vincentkibet, key: POSTGRES_USER }
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef: { name: ecomm-secrets-vincentkibet, key: POSTGRES_PASSWORD }
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef: { name: ecomm-config-vincentkibet, key: DATABASE_NAME }
        - name: RABBITMQ_HOST
          valueFrom:
            configMapKeyRef: { name: ecomm-config-vincentkibet, key: RABBITMQ_HOST }
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef: { name: ecomm-secrets-vincentkibet, key: RABBITMQ_USER }
        - name: RABBITMQ_PASS
          valueFrom:
            secretKeyRef: { name: ecomm-secrets-vincentkibet, key: RABBITMQ_PASS }
        - name: CUSTOMER_SERVICE_URL
          valueFrom:
            configMapKeyRef: { name: ecomm-config-vincentkibet, key: CUSTOMER_SERVICE_URL }
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  # Internal service
  type: ClusterIP